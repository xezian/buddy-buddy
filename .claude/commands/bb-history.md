Show buddy journal history. Accepts an optional number of entries to display.

$ARGUMENTS contains the optional N provided by the user (e.g. `/bb-history 10`).

Run:
```bash
~/.claude/buddy/bin/bb-history $ARGUMENTS
```

Output the script's stdout exactly as-is, preserving every line and newline. No summarizing, no truncating, no commentary.
